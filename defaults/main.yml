---
# defaults file for afonsog.k8s_jenkins

jenkins_enabled: true
jenkins_version: 2.176.3 
jenkins_values: 
  master:
    adminUser: geronimo
    adminPassword: jenkins
    jenkinsAdminEmail: geronimo.afonso@mikroways.net
    serviceAnnotations:
      annotations:
        external-dns.alpha.kubernetes.io/hostname: nginx.test.geronimo.com
